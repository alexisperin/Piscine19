#include "ft_tail.h"

int main(int argc, char **argv)
{
    if (argc < 3)
        display_error(argv[0], "", 1);
    else if (ft_strcmp(argv[1], "-c") !0)
        display_error(argv[0], "", 2);
    else
    {
        ft_tail()
    }
}